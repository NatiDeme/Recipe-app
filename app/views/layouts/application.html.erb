<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="Nav-bar">
  <p class="nav-link"><%= link_to 'Foods', root_path%></p>
  <p class="nav-link"><%= link_to 'Recipes', recipes_path%></p>
  <p class="nav-link"><%= link_to 'Public Recipes', public_recipes_path%></p>
  <% if user_signed_in? %>
  <p class="nav-link"><%= button_to 'Logout', destroy_user_session_path, method: :delete  %></p>
  <%end%>
  </div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p> 
    <%= yield %>
  </body>
</html>
